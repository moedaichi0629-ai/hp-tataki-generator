# Googleマップ店舗情報収集・管理ツール

Googleマップから店舗情報を収集し、HP制作候補の店舗を整理・編集・管理するWebアプリです。

- **第1段階**: 旧バージョンの「HPたたき台自動生成」機能を廃止し、店舗情報の収集・保存・編集・管理に特化した構成へ全面刷新
- **第2段階**: HP作成AIへ渡すための画像情報（Googleマップ写真・アップロード画像・外部URL・SNS投稿メモ）を収集・選択・整理する機能を追加
- **第3段階**: HP制作条件・AIツール別のHP作成用プロンプトを生成する機能を追加したが、運用上プロンプト作成が不要になったため、コード・API・画面をすべて削除。店舗詳細のタブ表示を「基本情報」のみに縮小（サービス・メニュー／口コミ・強み／画像管理／HP制作条件／メモ・管理情報のデータ・APIは温存し、画面上のみ非表示）
- **第4段階（今回）**: 店舗一覧のExcelエクスポート、店舗検索結果からの複数選択・一括登録、検索結果の画面遷移をまたいだ保持、作成したHPのリンク・営業済みチェック・登録時の検索条件（地域・検索範囲）の記録機能を追加

**このアプリはHP自体・HTML・デザイン・プレビューを一切生成しません。** 店舗情報の収集・管理・エクスポートに特化したツールです。

## アプリURL

https://hp-tataki-generator-s74r.vercel.app/

> Google Places APIの利用料金が発生するため、デモ環境でのAPIキーは常時有効化していない場合があります。また、データベース（Supabase）が未接続の状態ではエラーメッセージが表示されます（下記「セットアップ方法」参照）。

## 実装した機能

### 第1段階

- 店舗検索（地域×業種の一括検索／GoogleマップURLからの個別登録／店名・住所からの個別検索）
- 店舗情報の保存・編集（基本情報・外部リンク・Googleマップ情報・管理情報）
- 店舗一覧（検索・絞り込み・並び替え・ステータス変更・削除・重複確認）
- サービス・メニュー管理、口コミ表示・強み整理（AI下書き機能つき）、メモ機能
- 店舗ステータス・公式サイト確認状態・情報確認状態の管理、重複店舗の防止
- ダッシュボード、旧バージョンからのデータ移行機能

### 第2段階

- 店舗詳細に「画像管理」機能を追加（Googleマップ写真／アップロード画像／外部画像URL／SNS投稿メモ／使用画像一覧の5セクション構成）
- Google Places APIによるGoogleマップ掲載写真の参照情報取得（最大10枚、実体は保存せず都度取得）
- ブラウザからSupabase Storageへの直接アップロード（複数ファイル対応、進捗表示、破損・重複チェック）
- 外部画像URLの参照登録（SSRF対策つきの到達性・Content-Type確認、ダウンロードはしない）
- SNS投稿URLのメモ管理（自動取得は行わない）
- 画像ごとの画像種別・使用用途（複数選択可）・利用確認ステータス・表示順の管理
- HP制作向け画像準備状況のチェック（枚数集計・警告表示）

> 「サービス・メニュー」「口コミ・強み」「画像管理」「HP制作条件」「メモ・管理情報」のデータ・APIは残っていますが、店舗詳細画面では現在「基本情報」タブのみを表示しています（運用上プロンプト作成が不要になったことに伴う変更）。

### 第4段階（今回追加）

- **店舗一覧のExcelエクスポート**: 現在の絞り込み条件を反映した状態で、店舗名・Googleマップのリンク・地域（登録時の検索条件）・業種・作成したHPのリンク・営業済み・検索範囲の7列をxlsx形式でダウンロード
- **検索結果からの一括登録**: 地域×業種検索／GoogleマップURL登録／店名・住所検索のいずれの結果一覧でも、複数の候補にチェックを入れて「まとめて登録する」ことが可能（重複の可能性がある候補は自動登録せず、個別確認を促す）
- **検索結果の保持**: 店舗検索画面から店舗一覧などへ移動したあと、再度「店舗検索」に戻っても直前の検索条件・検索結果・登録済み状態がそのまま復元される（`sessionStorage`を使用。ブラウザタブを閉じると消える）
- **作成したHPのリンク**: 店舗ごとにHP完成後のURLを記録（基本情報の編集画面から入力）
- **営業済みチェック**: 既存の「営業対象/対象外」とは別に、営業を実施したかどうかを店舗一覧から即座にチェックできる項目を追加
- **登録時の検索条件の記録**: 地域×業種検索で店舗を登録した場合、その時に使った地域・検索範囲を店舗にスナップショットとして保存（GoogleマップURL登録・店名住所検索経由の店舗ではこの2項目は空欄）

## 使用技術

- Next.js（App Router） / React / TypeScript
- CSS Modules
- Supabase（Postgres + Storage）… 店舗・画像・HP制作条件データの永続化とアップロード画像の保管
- Google Places API（Text Search / Nearby Search / Place Details / Place Photos）、Google Geocoding API
- OpenAI API（口コミ・店舗情報からの「強み整理」下書き生成にのみ使用）
- ExcelJS（店舗一覧のxlsxエクスポート）
- Zod（入力値バリデーション）

## 機能一覧

### 店舗検索から登録・管理までの流れ

1. 店舗検索（地域×業種一括検索／GoogleマップURL登録／店名・住所検索）で店舗を1件ずつ、または複数選択してまとめて登録
2. 店舗情報管理（基本情報編集、ステータス管理、重複防止）
3. サービス・メニュー、口コミ・強み整理、画像管理、HP制作条件の入力（画面は非表示だがAPIからは操作可能）
4. 作成したHPのリンク・営業済みチェックを記録
5. 店舗一覧をExcelにエクスポートして営業管理に活用

### 店舗の登録方法（GoogleマップURL登録を含む）

1. 地域×業種による一括検索（取得件数・最低評価・最低口コミ数・営業中のみ・公式サイトなし候補のみ・登録済み除外の各フィルタに対応）
2. GoogleマップURLからの個別登録（通常URL・短縮URL `maps.app.goo.gl` に対応。Place IDを直接特定できない場合は店名・座標からText Search / Nearby Searchで候補を検索し、複数候補がある場合は選択可能。SSRF対策あり）
3. 店名・住所からの個別検索

いずれの検索結果一覧でも、候補にチェックを入れて「選択した◯件をまとめて登録する」ことができます（重複疑いの候補は自動登録せず、個別カードから確認・登録します）。

### 店舗情報管理

- 店舗一覧：店舗名・地域・業種・評価・口コミ数・公式サイト確認状態・店舗ステータス・営業対象/対象外・営業済み・登録日・更新日で絞り込み。6種類の並び替えに対応
- 店舗詳細：基本情報タブ（外部リンク・Googleマップ情報・登録時の検索条件を含む）
- 店舗ステータス14種類（候補〜成約・見送り・対象外）
- 公式サイト確認状態6種類。Googleマップの情報だけで「公式サイトなし」と断定しない設計
- 基本情報は項目ごとに確認状態（未確認/確認済み/要修正/掲載しない）を設定可能
- 重複防止：Place ID完全一致を最優先とし、Place IDが無い場合は店名・住所・電話番号・緯度経度（半径50m以内）で重複候補を検出（自動統合はしない）

### 口コミ・強み整理／画像管理／HP制作条件

第2・第3段階で実装した機能で、データ・APIは維持していますが、店舗詳細画面では現在非表示です。

## 実装手順

### 第1段階

1. 既存プロジェクト構成の調査
2. 削除対象と残す対象の整理
3. Supabaseへのデータベース設計・マイグレーションSQL作成
4. HP生成関連のAPI Route・ライブラリ・型定義・UIの削除
5. 店舗検索APIの拡張、店舗CRUD・重複判定の実装
6. ダッシュボード・店舗一覧・店舗詳細・編集・設定の各画面実装
7. 全API Routeでのエラーハンドリング統一
8. TypeScriptチェック・ESLint・ビルド確認、README更新

### 第2段階

1. 第1段階後のコード構成（店舗詳細タブ構成・DB・Google Places API利用状況・ストレージ有無）を確認
2. 画像データ構造（`store_images` / `store_social_image_references`）を設計し、Supabase Storageバケットとあわせてマイグレーション作成
3. Google Places APIの写真参照取得処理・サーバープロキシ配信を実装
4. 署名付きアップロードURL方式でのブラウザ直アップロードを実装（Vercelのリクエストボディ上限を回避するため）
5. 外部URL確認（SSRF対策）・SNS投稿メモ・画像CRUD・並び替え・一括操作のAPIを実装
6. 画像管理タブのUI（5セクション＋プレビュー＋準備状況パネル）を実装し店舗詳細へ組み込み
7. 店舗削除時のStorageクリーンアップを追加
8. TypeScriptチェック・ESLint・ビルド確認、実際のSupabaseに対するAPI疎通確認、README更新

### 第4段階

1. 店舗検索の3タブ（地域×業種／GoogleマップURL／店名住所）に複数選択・一括登録UIを追加し、共通の登録処理を`src/lib/registerPlace.ts`に切り出し
2. 検索フォーム・検索結果・選択中タブを`sessionStorage`に保存し、画面遷移後も復元できるようにする仕組みを追加
3. `stores`テーブルに`created_hp_url`・`sales_contacted`・`registration_region`・`registration_search_radius_meters`を追加するマイグレーションを作成
4. ExcelJSを導入し、店舗一覧の絞り込み条件を反映したxlsxを生成するAPI Routeを実装
5. 店舗詳細・編集画面・店舗一覧に新規フィールドを反映
6. TypeScriptチェック・ESLint・ビルド確認、実際のSupabaseに対する動作確認、README更新

## ディレクトリ構成

```
hp-tataki-generator/
├── .env.local.example              # 環境変数テンプレート
├── supabase/
│   └── migrations/
│       ├── 0001_init.sql           # 店舗関連テーブル（stores, store_services, store_reviews,
│       │                           #   store_strengths, store_notes, search_histories）
│       ├── 0002_store_images.sql   # 画像関連テーブル（store_images, store_social_image_references）
│       │                           #   + Supabase Storageバケット(store-images)
│       ├── 0003_prompts.sql        # HP制作条件関連テーブル（website_requirements, website_sections）
│       │                           #   ※プロンプト関連テーブル（generated_prompts等）はアプリからは未使用
│       ├── 0004_fix_store_reviews_dedupe.sql
│       └── 0005_store_extra_fields.sql  # 作成HPリンク・営業済み・登録時検索条件カラムを追加
├── src/
│   ├── app/
│   │   ├── page.tsx                 # ダッシュボード
│   │   ├── search/                  # 店舗検索（3方式のタブ + 検索履歴 + 一括登録）
│   │   ├── stores/
│   │   │   ├── page.tsx             # 店舗一覧（Excelエクスポート含む）
│   │   │   └── [id]/
│   │   │       ├── page.tsx         # 店舗詳細（基本情報タブ）
│   │   │       ├── edit/            # 店舗情報編集
│   │   │       ├── images/          # 画像管理（画面は現在非表示）
│   │   │       └── requirements/    # HP制作条件（画面は現在非表示）
│   │   ├── settings/                # 環境変数状況・データ移行
│   │   └── api/
│   │       ├── search/              # 地域×業種検索・URL解決・店名検索
│   │       ├── stores/
│   │       │   ├── export/          # 店舗一覧のExcelエクスポート
│   │       │   └── [id]/
│   │       │       ├── images/                # 画像CRUD・Googleマップ写真取得・写真プロキシ・
│   │       │       │                          #   アップロードURL発行・外部URL確認・並び替え・一括操作
│   │       │       ├── social-image-references/  # SNS投稿メモCRUD
│   │       │       └── website-requirements/   # HP制作条件・セクションCRUD
│   │       ├── search-histories/
│   │       ├── dashboard/
│   │       ├── migrate/             # 旧localStorageデータの移行
│   │       └── settings/            # 環境変数設定状況
│   ├── components/                  # AppShell, StoreStatusBadge, ConfirmDialog など共通UI
│   ├── lib/
│   │   ├── registerPlace.ts         # 店舗登録処理の共通ロジック（単体登録・一括登録の両方から利用）
│   │   ├── sessionState.ts          # 検索状態のsessionStorage保存・復元
│   │   ├── searchRadiusOptions.ts   # 検索範囲の選択肢・表示ラベル整形
│   │   └── （Supabaseクライアント、店舗/画像マッパー、重複判定、
│   │        GoogleマップURL解決・外部URL安全性検証（SSRF対策）、アップロード検証等）
│   ├── styles/common.module.css     # 全画面共通のUIスタイル
│   └── types/                       # ドメイン型・Supabaseテーブル型
└── tsconfig.json
```

## セットアップ方法

1. 依存パッケージをインストール

   ```
   npm install
   ```

2. `.env.local.example` をコピーして `.env.local` を作成し、APIキー・Supabase接続情報を設定

3. Supabaseプロジェクトを作成し、SQL Editorで `supabase/migrations/` 配下のファイルを番号順（`0001` → `0005`）に実行（`0002`はSupabase Storageのバケット`store-images`も作成します）

4. 開発サーバーを起動

   ```
   npm run dev
   ```

## 環境変数

```
GOOGLE_PLACES_API_KEY=          # Google Places API・Geocoding APIキー
OPENAI_API_KEY=                 # OpenAI APIキー（強み整理の下書き生成に使用）
OPENAI_MODEL=gpt-4o-mini
SUPABASE_URL=                   # SupabaseプロジェクトのURL（サーバー専用）
SUPABASE_SERVICE_ROLE_KEY=      # Supabaseのservice_roleキー（サーバー専用・秘密）
NEXT_PUBLIC_SUPABASE_URL=       # SUPABASE_URLと同じ値（ブラウザから直接アップロードするため必要）
NEXT_PUBLIC_SUPABASE_ANON_KEY=  # Supabaseのanon/publishableキー（公開可能な鍵）
```

`SUPABASE_SERVICE_ROLE_KEY`はAPI Routeでのみ使用し、クライアントに送出しません。`NEXT_PUBLIC_`が付く2つは、ブラウザからSupabase Storageへ直接ファイルをアップロードするために必要な、公開しても問題ない鍵です（実際のアップロード先パスは、サーバーが発行する署名付きURLでのみ許可されます）。

## データベース構成

Supabase（Postgres）に以下のテーブルを保持しています。

- `stores` / `store_services` / `store_reviews` / `store_strengths` / `store_notes` / `search_histories`（第1段階）
- `store_images` / `store_social_image_references`（第2段階） + Storageバケット`store-images`
- `website_requirements`（店舗ごとのHP制作条件、1店舗1行） / `website_sections`（セクション構成）（第3段階。画面は現在非表示）
- `generated_prompts` / `prompt_templates`（第3段階で作成したプロンプト関連テーブル。アプリのコードからは現在使用していません）

`stores`テーブルには第4段階で`created_hp_url`（作成したHPのURL）・`sales_contacted`（営業済みフラグ）・`registration_region`（登録時の検索地域）・`registration_search_radius_meters`（登録時の検索範囲）を追加しています。

## 画像利用上の注意

各画像には以下の利用確認ステータスを設定できます（画面は現在非表示ですがデータ・APIは維持しています）。

未確認 / 営業提案用のみ / 店舗から使用許可あり / 店舗提供画像 / 自分で用意した画像 / フリー素材 / AI生成画像 / 正式公開使用可能 / 使用不可

## Google Maps Platform利用上の注意

- Google Places APIから取得した口コミ・写真には、Googleマップ由来である旨の帰属表示を画面上に維持しています（削除・改変しません）
- 口コミ本文をHP掲載用文章としてそのまま転載する機能は実装していません
- Googleマップ写真の実体（バイナリ）は自前のストレージへ保存せず、表示のたびにGoogle Places APIから取得します（詳細は下記）

### Google Places API写真取得の仕組み

- レガシーPlaces API（`maps.googleapis.com/maps/api/place/*`）のPlace Detailsの`photos`フィールドのみを要求するfield mask（`place_id,photos`）で取得します
- 取得するのは`photo_reference`（写真参照情報）・幅・高さ・投稿者情報のみで、画像の実体は一切ダウンロード・保存しません
- 画面での表示は、サーバー側プロキシ（`GET /api/stores/[id]/images/google-photo`）が都度Google Photo APIを呼び出して配信します
- 「写真情報を取得」ボタンは新規取得・再取得の両方を兼ね、`photo_reference`が期限切れになっていた場合も再取得により最新化されます

### 画像アップロードの仕組み

- Vercelのサーバーレス関数にはリクエストボディに約4.5MBの上限があり、推奨の「10MB以下」をサーバー経由アップロードでは満たせません
- そのため、サーバーはSupabase Storageの署名付きアップロードURLを発行するだけに留め、ブラウザから直接Supabase Storageへアップロードします（サーバーはファイルの実バイトを一切扱いません）
- 破損チェックと画像サイズの取得は、ブラウザの`Image()`デコード（失敗時＝破損とみなす）を利用しています
- アップロード先のバケットには、Supabase側で`file_size_limit`（10MB）と`allowed_mime_types`（JPEG/PNG/WebP）を設定し、ストレージ側でも制限を強制しています

## 工夫した点

- **重複登録の防止**：Place ID完全一致を最優先とし、Place IDが無い場合も店名・住所・電話番号・緯度経度（半径50m）の組み合わせで近似判定を行い、自動統合はせずユーザーに確認を委ねる設計にした
- **SSRF対策の使い分け**：GoogleマップURLの短縮URL展開はGoogle系ドメインへの許可リスト方式、外部画像URLはDNS解決したIPアドレスがプライベート/ループバック帯域でないことを検証する方式と、URLの性質に応じて適切な対策を使い分けている
- **DB未接続時の耐障害性**：Supabase未設定時にAPI Routeが例外を投げても、必ず日本語のJSONエラーを返すよう全ルートでハンドリングを統一し、画面がクラッシュしないようにした
- **画像の非保存設計**：Googleマップ写真は参照情報のみ保存しプロキシ配信、外部URL・SNS投稿も参照のみで、実体を持つのはユーザーが明示的にアップロードした画像だけという一貫した方針にした
- **一括登録の安全側設計**：一括登録では完全一致の重複のみ自動でスキップし、重複疑いの候補は自動登録せず個別確認に委ねることで、意図しない重複登録を防いだ
- **検索状態の永続化**：検索フォーム・結果・登録済みフラグをタブごとに`sessionStorage`へ保存する設計にし、画面遷移をまたいでも検索し直す手間をなくした（タブを閉じれば消えるため、古い検索結果が無期限に残る問題も避けている）
- **登録経路のスナップショット化**：地域・検索範囲は本来「検索時のパラメータ」で店舗には属さない情報だが、Excel出力で営業管理に使いたいという要望に対し、登録時点の値を店舗にコピーして保持する設計にした（検索条件は店舗登録後に変わりうるため、後から動的に算出せずスナップショットとして固定）

## 今後追加したい機能

- Excelエクスポートの列カスタマイズ（表示する列の選択）
- 営業履歴・商談メモの時系列管理
- 店舗一覧からのステータス一括変更

## 学んだこと

- 単一ページの生成ツールを、複数テーブルにまたがる管理システムへ移行する際は、削除範囲と残す範囲を先に明文化してから着手すると手戻りが少ない
- 「Googleマップに公式サイトがない＝実在しない」と断定しない設計（確認状態を多段階で持つ）は、営業判断を誤らせないための重要な安全策になる
- サーバーレス環境では、DB未接続などの初期化エラーが同期的にthrowされるケースを想定し、全APIルートで一貫したエラーハンドリングを行う必要がある
- Vercelのようなサーバーレス環境でファイルアップロードを扱う場合、リクエストボディサイズの上限を早い段階で確認しておかないと、後から署名付きURL方式への設計変更が必要になる
- Supabase StorageのAPIゲートウェイは、署名付きアップロードURLへのリクエストであってもプロジェクトのAPIキー（anon/publishable）を要求するため、「サーバーの秘密鍵だけで完結する」という当初の想定が崩れることがある。ライブラリの内部実装（HTTPリクエストの実体）を確認してから設計する重要性を実感した
- 機能を丸ごと削除する際は、画面・API・型定義だけでなく、README等のドキュメントも同じタイミングで更新しないと、後から見たときにドキュメントとコードの状態が食い違ってしまう
- 「検索時のパラメータ」のように本来は特定のレコードに属さない情報でも、後から集計・出力に使いたいという要望が出た場合は、登録時点の値をスナップショットとして保存しておくのが実装コストと柔軟性のバランスが良い
